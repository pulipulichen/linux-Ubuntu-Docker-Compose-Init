version: "3"
services:
  app:
    build: .
    tty: true
    ports:
      - "80:8000"
    # environment:
    #   MYSQL_DATABASE: 'db'
    # command: node /app/zip-to-pdf.js "/input/資組.zip"
    # volumes: 
    #   - ./app:/app/
    #   - ./cache:/cache/
    #   - .:/input/